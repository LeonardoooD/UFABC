#include <stdio.h>/*(opcao = 1 para converter de decimal para bin´ario e opcao = 2 para
converter de bin´ario para decimal)*/

int main(){
    int opcao, valor, n, resto, resultado, base, expoente ;
    scanf("%d %d", &valor, &opcao);
    switch(opcao){
        case(1):{
            
        }
        case(2):{
            
        }
    }
    return 0;
}